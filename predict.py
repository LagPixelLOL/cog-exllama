from cog import BasePredictor, Input, Path

class Predictor(BasePredictor):
    
    def setup(self) -> None:
        return

    def predict(
        self,
    ) -> None:
        print("Hello, world!")
